% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MCubeRCTD.R
\name{mcubeRCTD}
\alias{mcubeRCTD}
\title{Directly integrating MCube with the cell type deconvolution results from RCTD}
\usage{
mcubeRCTD(
  RCTD_object,
  RCTD_mode,
  sample_size_max = 5000L,
  covariates = NULL,
  batch_id = NULL,
  spots = NULL,
  library_size_min = 10,
  celltype_test = NULL,
  gene_test = NULL,
  celltype_threshold = 100,
  gene_threshold = 5e-05,
  proportion_threshold = NULL,
  reference_threshold_test = 0.5,
  reference_threshold_fit = 0.25,
  safeguard = 1e-06,
  iter_max = 100L,
  tol = 1e-06,
  verbose = FALSE,
  kernels_list = NULL,
  standardize = TRUE,
  keep_kernels = FALSE,
  num_workers = 1L,
  num_threads = NULL,
  shared_memory = FALSE,
  project = "MCube"
)
}
\arguments{
\item{RCTD_object}{An S4 class used to represent spatial transcriptomic data and single-cell RNA-sequencing data,
run the \code{RCTD} algorithm, and store the cell type deconvolution results.
Please refer to the \code{RCTD} paper (\url{https://doi.org/10.1038/s41587-021-00830-w}) and the \code{spacexr} package (\url{https://github.com/dmcable/spacexr}) for more details.}

\item{RCTD_mode}{A character indicating in which mode to \code{RCTD}, corresponding to the \code{doublet_mode} argument in the \code{spacexr::run.RCTD()} function.
The choice of the \code{RCTD} mode depends on the resolution of the ST data and determines the analysis strategy of \code{MCube}.
For the low resolution ST data like Visium, we recommend to use the \code{full} mode.
Then, \code{MCube} will analyze all the cell types of interest together and store the results in a single \code{\linkS4class{mcube}} object.
For the high resolution ST data with large sample size like Visium HD and Xenium, we recommend to use the \code{doublet} mode.
Then, \code{MCube} will analyze each cell type separately using the spots with a high probability of containing the target cell type according to the deconvolution results.
The results will be stored in a list of \code{\linkS4class{mcube}} objects, each corresponding to a cell type.}

\item{sample_size_max}{A positive integer.
The high resolution ST data may contain over 100K samples in total and lead to high computational costs.
Therefore, in the \code{doublet} mode, we randomly sample \code{sample_size_max} spots to analyze for the cell type with valid spots greater than \code{sample_size_max}.}

\item{covariates}{A matrix.
Each row represents a spot and each column represents a covariate.}

\item{batch_id}{A character/factor vector indicating which batch each spot comes from.
It's applicable to the case of multiple samples/replicates/slices and specific gene platform effects required.
If \code{NULL}, all spots will be assumed to come from the same batch and share the same gene platform effects. Default is \code{NULL}.}

\item{spots}{A character vector specifying the names of spots to analyze.}

\item{library_size_min}{A numeric value.
The minimum library size to filter out spots.}

\item{celltype_test}{A character vector specifying the cell types to test.
If \code{NULL}, all cell types that pass the filtering criteria will be included. Default is \code{NULL}.}

\item{gene_test}{A character vector specifying the genes to test.
If \code{NULL}, all genes that pass the filtering criteria will be included. Default is \code{NULL}.}

\item{celltype_threshold}{A numeric value.
The minimum proportion sum across spots of a cell type to be considered.}

\item{gene_threshold}{A numeric value.
The minimum average expression level across spots of a gene to be considered.}

\item{proportion_threshold}{A numeric value between 0 and 1.
The minimum proportion of a cell type to be considered at a spot.
If not specified, it will be set as 0.1 in the \code{full} mode and 0.01 in the \code{doublet} mode.}

\item{reference_threshold_test}{A numeric value between 0 and 1.
The minimum relative expression level of a gene to be considered for a cell type in testing.}

\item{reference_threshold_fit}{A numeric value between 0 and 1.
The minimum relative expression level of a gene to be considered for a cell type when fitting the null models.}

\item{safeguard}{A numeric value.
A small positive number to avoid numerical issues when computing the inverse of a matrix for fitting the null models.}

\item{iter_max}{A positive integer.
The maximum number of iterations for the optimization algorithm when fitting the null models.}

\item{tol}{A numeric value.
The convergence criteria for the optimization algorithm when fitting the null models.}

\item{verbose}{A logical value.
Whether to print the progress of the optimization algorithm when fitting the null models. Default is FALSE.}

\item{kernels_list}{A list containing the kernel matrices for testing.
If \code{NULL}, the kernel matrices will be generated by the function \code{mcubeKernel}. Default is \code{NULL}.}

\item{standardize}{A logical value.
If \code{TRUE}, the coordinates will be standardized. Default is \code{TRUE}.}

\item{keep_kernels}{A logical value indicating whether to store kernels after testing. Default is \code{FALSE}.}

\item{num_workers}{A positive integer.
The number of worker processes for parallel computing. Default is 1.}

\item{num_threads}{A positive integer.
The number of threads on BLAS per worker.}

\item{shared_memory}{A logical value.
If \code{TRUE}, the large kernel matrices will be handled through memory-mapping to binary files on disk.}

\item{project}{A character recording the name of the project.}
}
\value{
A single \code{\linkS4class{mcube}} object if \code{RCTD_mode} is \code{full},
and a list of \code{\linkS4class{mcube}} objects with each element corresponding to a cell type if \code{RCTD_mode} is \code{doublet}.
}
\description{
Directly integrating MCube with the cell type deconvolution results from RCTD
}
