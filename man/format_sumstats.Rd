% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MCubeHelpers.R
\name{format_sumstats}
\alias{format_sumstats}
\title{Format the GWAS summary statistics data}
\usage{
format_sumstats(
  sumstats,
  delim = NULL,
  snps_merge = NULL,
  snps_mhc = NULL,
  dbSNP = NULL,
  snp_col = "SNP",
  beta_col = "BETA",
  or_col = "OR",
  se_col = "SE",
  freq_col = "FREQ",
  a1_col = "A1",
  a2_col = "A2",
  p_col = "P",
  n_col = "N",
  ncase_col = "N_CASE",
  ncontrol_col = "N_CONTROL",
  n = NULL,
  z_col = "Z",
  info_col = "INFO",
  log_pval = FALSE,
  chi2_max = NULL,
  min_freq = 0.05
)
}
\arguments{
\item{sumstats}{Summary statistics data or name of the summary statistics data file.}

\item{snps_merge}{A data.frame with SNPs to extract.}

\item{snps_mhc}{A set of SNPs that needed to be removed. For example, SNPs in MHC region.}

\item{dbSNP}{A tibble with two columns \code{rsID} = (rs) identifiers and \code{SNP} = chr:location. The data is from the dbSNP153 database (https://genome.ucsc.edu/FAQ/FAQdownloads.html#snp).}

\item{snp_col}{Name of a column with rs numbers. The default is \code{SNP}.}

\item{beta_col}{Name of a column with effect sizes (= log(or)). The default is \code{BETA}.}

\item{or_col}{Name of a column with odds ratios. The default is \code{OR}.}

\item{se_col}{Name of a column with standard errors. The default is \code{SE}.}

\item{freq_col}{Name of a column with effect allele frequencies. The default is \code{FREQ}.}

\item{a1_col}{Name of a column with effect alleles. The default is \code{A1}.}

\item{a2_col}{Name of a column with the other alleles. The default is \code{A2}.}

\item{p_col}{Name of a column with p-values. The default is \code{P}.}

\item{n_col}{Name of a column with sample sizes. The default is \code{N}.}

\item{ncase_col}{Name of a column with the number of cases. The default is \code{N_CASE}.}

\item{ncontrol_col}{Name of a column with the number of controls. The default is \code{N_CONTROL}.}

\item{n}{Sample size. If the column for sample size is not available, users can use argument "n" to specify the total sample size for each SNP.}

\item{z_col}{Name of a column with z scores. The default is \code{Z}.}

\item{info_col}{Name of a column with imputation INFO. The default is \code{INFO}.}

\item{log_pval}{Logical, whether the p-value is in -log10 scale. The default is \code{FALSE}.}

\item{chi2_max}{SNPs with tested chi^2 statistics large than chi2_max will be removed. The default is \code{80}.}

\item{min_freq}{SNPs with allele frequency less than min_freq will be removed. The default is \code{0.05}.}
}
\value{
Formatted summary statistics data
}
\description{
Format the GWAS summary statistics data
}
\details{
Format the GWAS summary statistics data for linkage disequilibrium score regression (LDSC) (\url{https://doi.org/10.1038/ng.3211}).
Credit goes to \code{ldsc} (\url{https://github.com/bulik/ldsc}) and the R package \code{MR-APSS} (\url{https://github.com/YangLabHKUST/MR-APSS}).
}
