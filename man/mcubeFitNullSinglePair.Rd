% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MCubeFitNull.R
\name{mcubeFitNullSinglePair}
\alias{mcubeFitNullSinglePair}
\title{Fit the null MMM model}
\usage{
mcubeFitNullSinglePair(
  Y,
  library_sizes,
  X = NULL,
  proportions,
  batch_id = NULL,
  reference,
  used_for_deconvolution = TRUE,
  spot_effects = NULL,
  platform_effect = NULL,
  celltype_test,
  proportion_threshold = 0.1,
  reference_threshold = 0.25,
  safeguard = 1e-06,
  iter_max = 100L,
  tol = 1e-06,
  verbose = TRUE,
  num_threads = NULL
)
}
\arguments{
\item{Y}{A numeric vector containing gene expression counts of all spots.}

\item{library_sizes}{A numeric vector containing library sizes of all spots.}

\item{X}{A numeric matrix containing covariates of all spots.
Each row represents a spot and each column represents a covariate.
If \code{NULL}, a matrix with one column of all 1s will be used as the covariate matrix. Default is \code{NULL}.}

\item{proportions}{A numeric matrix containing cell type proportions of all spots.
Each row represents a spot and each column represents a cell type.}

\item{batch_id}{A character/factor vector indicating which batch each spot comes from.
It's applicable to the case of multiple samples/replicates/slices and specific gene platform effects required.
If \code{NULL}, all spots will be assumed to come from the same batch and share the same gene platform effects. Default is \code{NULL}.}

\item{reference}{A vector of average gene expression calculated from scRNA-seq reference data with each element corresponding to a cell type.}

\item{used_for_deconvolution}{A logical value indicating whether the gene has been used for cell type deconvolution in the previous step.}

\item{spot_effects}{A numeric vector of spot effects with each element corresponding to a spot.}

\item{platform_effect}{A numeric value or vector.
In the single batch case, a numeric value is expected.
When in the case of multiple batches and specific platform effects required, a vector is expected with each element corresponding to a batch.
If \code{NULL}, the platform effect will be estimated from data with zero initialization. Default is \code{NULL}.}

\item{celltype_test}{A character specifying the cell type to test after fitting the null model.}

\item{proportion_threshold}{A numeric value between 0 and 1.
The minimum proportion of a cell type at a spot to be considered.}

\item{reference_threshold}{A numeric value between 0 and 1.
The minimum relative gene expression level in a cell type to be considered for fitting the null model.}

\item{safeguard}{A numeric value.
A small positive number to avoid numerical issues when computing the inverse of a matrix.}

\item{iter_max}{A positive integer.
The maximum number of iterations for the optimization algorithm.}

\item{tol}{A numeric value.
The convergence criteria for the optimization algorithm.}

\item{verbose}{A logical value.
Whether to print the progress of the optimization algorithm. Default is TRUE.}

\item{num_threads}{A positive integer.
The number of threads for BLAS operations.}
}
\value{
A list containing the fitted null model results.
}
\description{
Fit the null MMM model for a single celltype-gene pair using a PQL-based approach.
}
