% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MCubePlot.R
\name{mcubePlotExpr}
\alias{mcubePlotExpr}
\title{Plot the spatial distribution of gene expression}
\usage{
mcubePlotExpr(
  counts,
  coordinates,
  gene,
  spots = NULL,
  normalize = TRUE,
  he_image = NULL,
  background = TRUE,
  expr_threshold = 1,
  spot_size = 1,
  palettes = pals::brewer.bugn(20)[11:20],
  opacity_target = 1,
  opacity_background = 0.2,
  xlim = NULL,
  ylim = NULL,
  ratio = 1,
  title = NULL
)
}
\arguments{
\item{counts}{A matrix of gene expression counts.
Each row represents a spot and each column represents a gene.}

\item{coordinates}{A matrix of spatial coordinates.
Each row represents a spot and each column represents a spatial dimension.}

\item{gene}{A character specifying the gene to plot.}

\item{spots}{A character vector specifying the spots to plot.
If \code{NULL}, all spots will be plotted. Default is \code{NULL}.}

\item{normalize}{A logical value.
If \code{TRUE}, the gene expression will be log-transformed and then normalized to \link{0, 1}. Default is \code{TRUE}.}

\item{he_image}{H&E image background.
A H&E image in TIFF format can be read into a raster array using \code{tiff::readTIFF}. Default is \code{NULL}.}

\item{background}{A logical value.
If \code{TRUE}, the spots not pass the expression threshold will be plotted in grey as background. Default is \code{TRUE}.}

\item{expr_threshold}{A numeric value specifying the expression threshold.
Spots with expression values greater than or equal to the threshold will be plotted.
Other spots will be used as background if \code{background} is \code{TRUE}. Default is 1.}

\item{spot_size}{A numeric value specifying the size of spots. Default is 1.}

\item{palettes}{A vector of color palettes. Default is \code{pals::brewer.blues(20)}.}

\item{opacity_target}{A numeric value specifying the opacity of target spots. Default is 1.}

\item{opacity_background}{A numeric value specifying the opacity of background spots. Default is 0.2.}

\item{xlim}{A numeric vector specifying the x-axis limits. Default is \code{NULL}.}

\item{ylim}{A numeric vector specifying the y-axis limits. Default is \code{NULL}.}

\item{ratio}{A numeric value specifying the aspect ratio. Default is 1.}

\item{title}{A character specifying the plot title. Default is \code{NULL}.}
}
\value{
A ggplot object.
}
\description{
Plot the spatial distribution of gene expression
}
