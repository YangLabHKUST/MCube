% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MCubeClass.R
\name{createMCUBE}
\alias{createMCUBE}
\title{Create the \code{\linkS4class{MCUBE}} object}
\usage{
createMCUBE(
  counts,
  coordinates,
  proportions,
  library_sizes = NULL,
  covariates = NULL,
  batch_id = NULL,
  reference,
  used_for_deconvolution = NULL,
  spots = NULL,
  library_size_min = 10,
  spot_effects = NULL,
  platform_effects = NULL,
  celltype_test = NULL,
  gene_test = NULL,
  celltype_threshold = 100,
  gene_threshold = 5e-05,
  proportion_threshold = 0.1,
  reference_threshold = 0.5,
  project = "MCUBE"
)
}
\arguments{
\item{counts}{A matrix, Matrix::dgCMatrix, or Matrix::dgTMatrix.
Each Row represents a spot and each column represents a gene.}

\item{coordinates}{A matrix.
Each row represents a spot and each column represents a spatial dimension.}

\item{proportions}{A cell type proportion matrix.
Each row represents a spot and each column represents a cell type.}

\item{library_sizes}{A numeric vector containing the library sizes of all spots.}

\item{covariates}{A matrix.
Each row represents a spot and each column represents a covariate.}

\item{batch_id}{A character/factor vector indicating which batch each spot comes from.
It's applicable to the case of multiple samples/replicates/slices and specific gene platform effects required.
If \code{NULL}, all spots will be assumed to come from the same batch and share the same gene platform effects. Default is \code{NULL}.}

\item{reference}{A matrix of average gene expression calculated from scRNA-seq reference data.
Each row represents a cell type and each column represents a gene.}

\item{used_for_deconvolution}{A logical vector indicating whether each gene has been used for cell type deconvolution.
If \code{NULL}, all genes will be assumed have been used for cell type deconvolution. Default is \code{NULL}.}

\item{spots}{A character vector specifying the names of spots to analyze.}

\item{library_size_min}{A numeric value.
The minimum library size to filter out spots.}

\item{spot_effects}{A numeric vector containing the spot effects of all spots.}

\item{platform_effects}{A numeric vector or matrix containing the gene-specific platform effects.
In the single batch case, a numeric vector is expected with each element corresponding to a gene.
When in the case of multiple batches and specific platform effects required, a matrix is expected with rows corresponding to batches and columns corresponding to genes.
If \code{NULL}, the platform effects will be estimated from the data. Default is \code{NULL}.}

\item{celltype_test}{A character vector specifying the cell types to test.
If \code{NULL}, all cell types that pass the filtering criteria will be included. Default is \code{NULL}.}

\item{gene_test}{A character vector specifying the genes to test.
If \code{NULL}, all genes that pass the filtering criteria will be included. Default is \code{NULL}.}

\item{celltype_threshold}{A numeric value.
The minimum proportion sum across spots of a cell type to be considered.}

\item{gene_threshold}{A numeric value.
The minimum average expression level across spots of a gene to be considered.}

\item{proportion_threshold}{A numeric value between 0 and 1.
The minimum proportion of a cell type to be considered at a spot.}

\item{reference_threshold}{A numeric value between 0 and 1.
The minimum relative expression level of a gene to be considered for a cell type in testing.}

\item{project}{A character recording the name of the project.}
}
\value{
An \code{\linkS4class{MCUBE}} object.
}
\description{
Create the \code{\linkS4class{MCUBE}} object
}
